# OpenTelemetry Collector

Collect logs, metrics, and traces from Home Assistant Core, the Supervisor,
and every add-on container — all exported via OTLP to Grafana LGTM (Loki,
Tempo, Mimir) or any OTLP-compatible backend. Single add-on, no HACS
dependency, no custom components.
